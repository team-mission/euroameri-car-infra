# =======================================================
# 유로아메리카 인프라 환경변수 설정
# =======================================================

# 데이터베이스 설정
DB_HOST=mysql
DB_PORT=3306
DB_USERNAME=euroameri
DB_PASSWORD=strong_password_123
DB_NAME=euroamericar

# MySQL 루트 패스워드
MYSQL_ROOT_PASSWORD=root_password_456

# 애플리케이션 설정
NODE_ENV=development
BACKEND_PORT=4000
FRONTEND_PORT=3000

# 도메인 설정 (개발환경 - HTTPS)
DOMAIN=localhost
CLIENT_URL=https://localhost

# 프론트엔드에서 사용할 API URL
NEXT_PUBLIC_API_URL=https://localhost:4000

# 세션 시크릿 (실제 운영에서는 더 복잡한 키 사용)
SESSION_SECRET=euroameri_session_secret_key_change_in_production
COOKIE_SECRET=euroameri_cookie_secret_key_change_in_production

# =======================================================
# 프로덕션 환경용 설정 (장비 배포시 주석 해제하고 위 설정들 주석처리)
# =======================================================

# # 애플리케이션 설정 (프로덕션)
# NODE_ENV=production
# 
# # 도메인 설정 (프로덕션)
# DOMAIN=euroamericar.com
# CLIENT_URL=https://euroamericar.com
# 
# # 프론트엔드에서 사용할 API URL (프로덕션)
# NEXT_PUBLIC_API_URL=https://euroamericar.com
# 
# # 데이터베이스 설정 (프로덕션 - 필요시 변경)
# DB_PASSWORD=production_password_here
# MYSQL_ROOT_PASSWORD=production_root_password_here